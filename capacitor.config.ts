import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.ionic.productos.inventario',
  appName: 'Mi Iventario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
